<?php

declare(strict_types=1);

namespace /*{ServiceNamespace}*/;

use ReflectionClass;
use ReflectionMethod;
use Tbessenreither\MultiLevelCache\CachedServiceGenerator\Attribute\MlcCacheableMethod;
use Tbessenreither\MultiLevelCache\CachedServiceGenerator\Attribute\MlcCacheableService;
use Tbessenreither\MultiLevelCache\CachedServiceGenerator\Attribute\MlcCachedService;
use Tbessenreither\MultiLevelCache\CachedServiceGenerator\Dto\MethodCallObject;
use Tbessenreither\MultiLevelCache\CachedServiceGenerator\Service\KeyGeneratorService;
use Tbessenreither\MultiLevelCache\Factory\MultiLevelCacheFactory;
use Tbessenreither\MultiLevelCache\Service\MultiLevelCacheService;
use RuntimeException;
use Throwable;
/**
 * Dynamically added use statements
 */
/*{UseLines}*/

/**
 * Autogenerated by MLC CachedServiceGenerator
 * Do not edit this class manually
 *
 * Cache enabled wrapper for Service /*{ServiceName}*/.
 *
 * This Wrapper provides all public methods from the origin service and was generated using the make command.
 * You can regenerate/update using php vendor/tbessenreither/multi-level-cache/bin/make --service=/*{ClassDotSeparated}*/.
 * Or you can update all services with php vendor/tbessenreither/multi-level-cache/bin/update.
 * @codeCoverageIgnore
 * @mixin /*{ServiceName}*/
 */
#[MlcCachedService(
    originalServiceClass: /*{ServiceName}*/::class,
    allowSync: true,
)]
class /*{ServiceName}*/Cached implements /*{InterfacesString}*/
{
    public const CACHE_KEY_PREFIX = '/*{CacheKeyPrefix}*/';

    /**
     * @var array<string, MlcCacheableMethod|null>
     */
    private array $mlcCacheableMethodAttributeCache = [];
    private MultiLevelCacheService $cache;
    private ReflectionClass $originalClassReflection;
    private MlcCacheableService $mlcCacheableServiceAttribute;

    public function __construct(
        MultiLevelCacheFactory $multiLevelCacheFactory,
        private /*{ServiceName}*/ $instance,
    ) {
        $cacheGroupName = 'CachedService-/*{ClassUnderscoreSeparated}*/';

        $this->originalClassReflection = new ReflectionClass($instance);
        $this->mlcCacheableServiceAttribute = MlcCacheableService::fromReflectionClass($this->originalClassReflection);

        $mlcCacheableServiceAttribute = MlcCacheableService::fromReflectionClass(new ReflectionClass($instance));

        $this->cache = $multiLevelCacheFactory->createByType(
            type: $this->mlcCacheableServiceAttribute->getCacheType(),
            inMemoryCacheMaxSize: $this->mlcCacheableServiceAttribute->getInMemoryCacheMaxSize(),
            writeL0OnSet: true,
            cacheGroupName: $cacheGroupName,
        );
    }

    // Start Dynamically generated methods

/*{DynamicMethods}*/

    // End Dynamically generated methods

    private function callCachedMethod(string $name, array $args, bool $isStatic): mixed
    {
        return $this->cache->get(
            key: KeyGeneratorService::getKey(new MethodCallObject(
                class: $this->instance::class,
                method: $name,
                arguments: $args,
                additionalCacheKey: $this->getAdditionalCacheKey($name),
            )),
            callable: fn () => $this->callSource($name, $args, $isStatic),
            ttlSeconds: $this->getMlcCacheableMethodAttribute($name)->getTtlSeconds(),
        );
    }

    private function callCachedBulkMethod(string $name, array $args, bool $isStatic): array
    {
        return $this->cache->getBulk(
            methodCallObject: new MethodCallObject(
                class: $isStatic ? $this->instance::class : $this->instance,
                method: $name,
                arguments: $args,
                additionalCacheKey: $this->getAdditionalCacheKey($name),
            ),
            mlcCacheableMethodAttribute: $this->getMlcCacheableMethodAttribute($name),
        );
    }

    private function callSource(string $name, array $args, bool $isStatic): mixed
    {
        if ($isStatic) {
            return ($this->instance::class)::{$name}(...$args);
        }

        return $this->instance->$name(...$args);
    }

    private function getMlcCacheableMethodAttribute(string $methodName): ?MlcCacheableMethod
    {
        if (!array_key_exists($methodName, $this->mlcCacheableMethodAttributeCache)) {
            $reflectionMethod = new ReflectionMethod($this->instance, $methodName);
            $this->mlcCacheableMethodAttributeCache[$methodName] = MlcCacheableMethod::fromReflectionMethod($reflectionMethod, false);
        }

        return $this->mlcCacheableMethodAttributeCache[$methodName];
    }

    private function getAdditionalCacheKey(string $methodName): ?string
    {
        $additionalIdGetter = $this->getMlcCacheableMethodAttribute($methodName)->getAdditionalCacheKeyGetter() ?? $this->mlcCacheableServiceAttribute->getAdditionalCacheKeyGetter();

        if (!$additionalIdGetter) {
            return null;
        }

        try {
            $getterResponse = $this->instance->{$additionalIdGetter}();
            return sha1(serialize($getterResponse));
        } catch (Throwable $e) {
            throw new RuntimeException(sprintf('An error occurred while calling the additional cache ID getter "%s": %s', $additionalIdGetter, $e->getMessage()), previous: $e);
        }

    }
}
